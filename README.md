# Minimal Audio Encoder
Reproducing something like the Encodec model but as simpler as possible to generate listenable results <br>
The idea is to use it as a didactic tool and to fork this repo to create a more complex version later 
